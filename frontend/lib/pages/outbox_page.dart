import 'package:flutter/material.dart';

class OutboxPage extends StatelessWidget {
  const OutboxPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('OutboxPage');
  }
}
